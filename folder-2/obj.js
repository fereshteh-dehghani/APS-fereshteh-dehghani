let user=new Object();  //created an  Empty object syntax1
let user2={};           //created an Empty object syntax2

let user={
    name:'Aida',
    age:20,
}
console.log(user.name,user.age);
delete user.age; //for delete a property we can use 'delete' operator;